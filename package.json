{
  "name": "mpls-grand-prix",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "MPLS Grand Prix — Svelte 5 + Vite, multijugador con PartyKit.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "party:dev": "partykit dev",
    "party:deploy": "partykit deploy",
    "pages:deploy": "wrangler pages deploy dist --project-name=mpls-grand-prix"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "partykit": "^0.0.114",
    "svelte": "^5.0.0",
    "vite": "^5.4.0",
    "wrangler": "^3.80.0"
  },
  "dependencies": {
    "partysocket": "^1.0.3"
  }
}
